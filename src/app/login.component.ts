import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  templateUrl: './login.component.html',
  styles: [`
    .modal-dialog {
        width: 350px;
    }
  `]
})

export class AppLoginComponent {
}
